<table>
  <tr>
    <th>Título</th>
    <th>Contenido</th>
  </tr>
  <% @posts.each do |post| %>
    <tr>
      <td><%= post.title %></td>
      <td><%= post.text %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Nuevo artículo', :action => :new %>